import React, { useState } from 'react';
import './Portfolio.css';

const BigImg = ({ className, src, onClick }) => {
    ////////Hooks////////////

    ////////Functions////////////

    /////////Jsx///////////
    return (
        <img className={className} src={src} onClick={onClick} alt="..." />
    )
}
export default BigImg 